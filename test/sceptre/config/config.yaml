---
project_code: cfntest
region: "{{ var.aws_region | default('us-west-2') }}"
git_repo: git@github.com:harisfauzi/shared-sceptre-template.git
j2_environment:
  lstrip_blocks: true
  # trim_blocks: false
  keep_trailing_newline: true
