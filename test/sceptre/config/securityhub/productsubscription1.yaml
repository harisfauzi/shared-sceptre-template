---
template:
  type: file
  path: securityhub/product_subscription.yaml.j2

sceptre_user_data:
  project_code: "{{ project_code }}"
  source_repo_url: "{{ git_repo }}"
  description: "SecurityHub Product Subscription(s) for testing purposes"
  product_subscriptions:
    - name: SplunkIntegration
      deletion_policy: Delete
      update_replace_policy: Delete
      product_arn: "arn:aws:securityhub:us-east-1::product/splunk/splunk-enterprise"

    - name: DatadogIntegration
      deletion_policy: Delete
      update_replace_policy: Delete
      product_arn: "arn:aws:securityhub:us-east-1::product/datadog/datadog-integration"

    - name: CrowdStrikeFalconIntegration
      deletion_policy: Delete
      update_replace_policy: Delete
      product_arn: "arn:aws:securityhub:us-east-1::product/crowdstrike/crowdstrike-falcon"

    - name: ServiceNowIntegration
      deletion_policy: Delete
      update_replace_policy: Delete
      product_arn: "arn:aws:securityhub:us-east-1::product/servicenow/servicenow-cmdb"

stack_tags:
  Environment: Test
  ManagedBy: Sceptre
  Test: securityhub-product-subscription
