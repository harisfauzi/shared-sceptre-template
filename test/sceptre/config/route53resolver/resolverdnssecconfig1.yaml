---
template:
  type: file
  path: route53resolver/resolver_dnssec_config.yaml.j2

sceptre_user_data:
  project_code: "{{project_code}}"
  source_repo_url: "{{git_repo}}"
  description: Sample Resolver DNSSEC Configurations for DNS Security Testing
  resolver_dns_sec_configs:
    - name: ProdDnssecConfig
      resource_id: vpc-0123456789abcdef0
      deletion_policy: Delete
      update_replace_policy: Delete
    - name: DevDnssecConfig
      resource_id: vpc-abcdef0123456789

stack_tags:
  Test: route53resolver-dnssec-config
