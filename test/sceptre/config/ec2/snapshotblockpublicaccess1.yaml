---
template:
  type: file
  path: ec2/snapshot_block_public_access.yaml.j2

sceptre_user_data:
  project_code: "{{ stack_group_config.project_code }}"
  source_repo_url: "{{git_repo}}"
  description: Sample Snapshot Block Public Access
  snapshot_block_public_access:
    name: SnapshotBlockPublicAccess1
    state: block-all-sharing
    deletion_policy: Retain

stack_tags:
  Test: ec2-snapshot-block-public-access
