---
template:
  type: file
  path: ec2/carrier_gateway.yaml.j2

sceptre_user_data:
  project_code: "{{stack_group_config.project_code}}"
  source_repo_url: "{{git_repo}}"
  carrier_gateways:
    - name: TestCarrierGateway
      vpc_id: vpc-0a1b2c3d4e5f67890
      tags:
        Name: TestCarrierGateway
        Environment: testing
        CostCenter: "12345"
      deletion_policy: Retain

stack_tags:
  Test: ec2-carriergateway
