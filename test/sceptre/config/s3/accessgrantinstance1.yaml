---
template:
  type: file
  path: s3/accessgrantinstance.yaml.j2

sceptre_user_data:
  project_code: "{{project_code}}"
  source_repo_url: "{{ git_repo }}"
  description: CloudFormation template to test Jinja2 template.
  access_grant_instances:
    - name: AccessGrantInstance1
      identity_center_arn: "arn:aws:identitystore::123456789012:identitystore/d-1234567890"
      tags:
        Name: AccessGrantInstance1
        Environment: Development
    - name: AccessGrantInstance2
      tags:
        Name: AccessGrantInstance2
        Environment: Production

stack_tags:
  Test: s3-accessgrantlocation
