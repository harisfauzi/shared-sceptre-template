{%- macro prep(vpc_settings) %}
VpcSettings:
  SubnetIds:
{%- for subnet_id in vpc_settings.subnet_ids %}
    - {{ subnet_id }trim }}
{%- endfor %}
  VpcId: {{ vpc_settings.vpc_id | trim }}
{%- endmacro %}